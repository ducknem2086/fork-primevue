import { style } from '@oda-components/style-tokens/checkboxgroup';
import BaseStyle from '@oda-components/core/base/style';

const classes = {
    root: 'p-checkbox-group p-component'
};

export default BaseStyle.extend({
    name: 'checkboxgroup',
    style,
    classes
});
