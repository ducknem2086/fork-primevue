import BaseDirective from '@app/oda-component/core/basedirective';
import TooltipStyle from '@app/oda-component/tooltip/style';

const BaseTooltip = BaseDirective.extend({
    style: TooltipStyle
});

export default BaseTooltip;
