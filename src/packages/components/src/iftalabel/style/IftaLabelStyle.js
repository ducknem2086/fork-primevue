import { style } from '@app/oda-component/style-tokens/iftalabel';
import BaseStyle from '@app/oda-component/core/base/style';

const classes = {
    root: 'p-iftalabel'
};

export default BaseStyle.extend({
    name: 'iftalabel',
    style,
    classes
});
