import { style } from '@oda-components/style-tokens/blockui';
import BaseStyle from '@oda-components/core/base/style';

const classes = {
    root: 'p-blockui'
};

export default BaseStyle.extend({
    name: 'blockui',
    style,
    classes
});
