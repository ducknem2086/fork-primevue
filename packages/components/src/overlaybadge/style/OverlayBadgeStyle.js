import { style } from '@oda-components/style-tokens/overlaybadge';
import BaseStyle from '@oda-components/core/base/style';

const classes = {
    root: 'p-overlaybadge'
};

export default BaseStyle.extend({
    name: 'overlaybadge',
    style,
    classes
});
