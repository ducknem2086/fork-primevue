import BaseDirective from '@oda-components/core/basedirective';
import FocusTrapStyle from 'oda-components/focustrap/style';

const BaseFocusTrap = BaseDirective.extend({
    style: FocusTrapStyle
});

export default BaseFocusTrap;
