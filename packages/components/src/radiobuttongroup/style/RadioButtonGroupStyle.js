import { style } from '@oda-components/style-tokens/radiobuttongroup';
import BaseStyle from '@oda-components/core/base/style';

const classes = {
    root: 'p-radiobutton-group p-component'
};

export default BaseStyle.extend({
    name: 'radiobuttongroup',
    style,
    classes
});
