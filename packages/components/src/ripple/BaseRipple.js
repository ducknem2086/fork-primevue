import BaseDirective from '@oda-components/core/basedirective';
import RippleStyle from 'oda-components/ripple/style';

const BaseRipple = BaseDirective.extend({
    style: RippleStyle
});

export default BaseRipple;
