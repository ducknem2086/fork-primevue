import { style } from '@app/oda-component/style-tokens/iconfield';
import BaseStyle from '@app/oda-component/core/base/style';

const classes = {
    root: 'p-iconfield'
};

export default BaseStyle.extend({
    name: 'iconfield',
    style,
    classes
});
