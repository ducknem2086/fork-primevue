import { style } from '@oda-components/style-tokens/iftalabel';
import BaseStyle from '@oda-components/core/base/style';

const classes = {
    root: 'p-iftalabel'
};

export default BaseStyle.extend({
    name: 'iftalabel',
    style,
    classes
});
