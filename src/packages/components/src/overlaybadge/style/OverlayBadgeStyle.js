import { style } from '@app/oda-component/style-tokens/overlaybadge';
import BaseStyle from '@app/oda-component/core/base/style';

const classes = {
    root: 'p-overlaybadge'
};

export default BaseStyle.extend({
    name: 'overlaybadge',
    style,
    classes
});
