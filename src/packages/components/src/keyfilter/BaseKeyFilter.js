import BaseDirective from '@app/oda-component/core/basedirective';
import KeyFilterStyle from '@app/oda-component/keyfilter/style';

const BaseKeyFilter = BaseDirective.extend({
    style: KeyFilterStyle
});

export default BaseKeyFilter;
