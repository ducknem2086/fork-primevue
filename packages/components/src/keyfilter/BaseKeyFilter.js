import BaseDirective from '@oda-components/core/basedirective';
import KeyFilterStyle from 'oda-components/keyfilter/style';

const BaseKeyFilter = BaseDirective.extend({
    style: KeyFilterStyle
});

export default BaseKeyFilter;
