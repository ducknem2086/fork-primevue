import { style } from '@oda-components/style-tokens/iconfield';
import BaseStyle from '@oda-components/core/base/style';

const classes = {
    root: 'p-iconfield'
};

export default BaseStyle.extend({
    name: 'iconfield',
    style,
    classes
});
