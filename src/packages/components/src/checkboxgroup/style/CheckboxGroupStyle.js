import { style } from '@app/oda-component/style-tokens/checkboxgroup';
import BaseStyle from '@app/oda-component/core/base/style';

const classes = {
    root: 'p-checkbox-group p-component'
};

export default BaseStyle.extend({
    name: 'checkboxgroup',
    style,
    classes
});
