import { style } from '@oda-components/style-tokens/inputgroup';
import BaseStyle from '@oda-components/core/base/style';

const classes = {
    root: 'p-inputgroup'
};

export default BaseStyle.extend({
    name: 'inputgroup',
    style,
    classes
});
