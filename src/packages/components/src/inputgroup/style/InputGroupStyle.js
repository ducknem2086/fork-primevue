import { style } from '@app/oda-component/style-tokens/inputgroup';
import BaseStyle from '@app/oda-component/core/base/style';

const classes = {
    root: 'p-inputgroup'
};

export default BaseStyle.extend({
    name: 'inputgroup',
    style,
    classes
});
