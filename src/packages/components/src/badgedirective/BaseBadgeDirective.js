import BaseDirective from '@app/oda-component/core/basedirective';
import BadgeDirectiveStyle from '@app/oda-component/badgedirective/style';

const BaseBadgeDirective = BaseDirective.extend({
    style: BadgeDirectiveStyle
});

export default BaseBadgeDirective;
