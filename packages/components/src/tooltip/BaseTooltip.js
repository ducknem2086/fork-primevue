import BaseDirective from '@oda-components/core/basedirective';
import TooltipStyle from 'oda-components/tooltip/style';

const BaseTooltip = BaseDirective.extend({
    style: TooltipStyle
});

export default BaseTooltip;
