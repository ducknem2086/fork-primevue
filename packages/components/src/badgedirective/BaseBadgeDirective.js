import BaseDirective from '@oda-components/core/basedirective';
import BadgeDirectiveStyle from 'oda-components/badgedirective/style';

const BaseBadgeDirective = BaseDirective.extend({
    style: BadgeDirectiveStyle
});

export default BaseBadgeDirective;
